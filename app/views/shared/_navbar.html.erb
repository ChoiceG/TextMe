<nav class="fixed top-0 left-0 right-0 bg-white shadow-md h-38 px-8 flex flex-col justify-center z-50">
    <!-- Brand -->
    <div class="text-3xl font-extrabold text-indigo-600 tracking-tight mb-4">
        <%= link_to "TextMe", root_path, class: "hover:text-indigo-800" %>
    </div>

    <!-- User info / Logout if logged in -->
    <% if current_user %>
    <div class="flex items-center justify-between">
        <span class="font-semibold text-gray-800 text-xl">
            <%= current_user.email %>
        </span>

        <%= button_to "Log Out", destroy_user_session_path, method: :delete,
            class: "bg-red-600 hover:bg-red-700 text-white font-semibold py-2 px-4 rounded transition" %>
    </div>
    <% end %>
</nav>