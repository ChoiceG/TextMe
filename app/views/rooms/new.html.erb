<div class="min-h-screen flex items-center justify-center bg-gray-100 py-8 px-4">
  <div class="max-w-md w-full bg-white p-6 rounded-lg shadow-md">
    <h2 class="text-2xl font-bold mb-6 text-center text-indigo-700">Create a New Room</h2>

    <%= form_with(model: @room, local: true) do |form| %>
      <div class="mb-4">
        <%= form.label :name, class: "block text-sm font-medium text-gray-700" %>
        <%= form.text_field :name, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:ring-indigo-500 focus:border-indigo-500" %>
      </div>

      <div class="mb-6">
        <%= form.label :description, class: "block text-sm font-medium text-gray-700" %>
        <%= form.text_area :description, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:ring-indigo-500 focus:border-indigo-500" %>
      </div>

      <%= form.submit "Create Room", class: "w-full bg-indigo-600 text-white py-2 px-4 rounded-md hover:bg-indigo-700" %>
    <% end %>
    <div class="text-center mt-4">
        <%= link_to "← Back to Rooms", rooms_path, class: "text-indigo-600 hover:underline text-sm" %>    
    </div>
  </div>
</div>
